# ============================================================
#  requirements-train.txt  —  Full training dependencies
#  Python 3.10  |  conda env: gesture-cls
#  Install AFTER creating the conda env (see README).
# ============================================================

# Inherit all inference deps
-r requirements.txt

# Training utilities
tqdm>=4.66
scikit-learn>=1.4
matplotlib>=3.8
seaborn>=0.13

# Experiment tracking (optional but recommended)
# tensorboard>=2.16

# Augmentation
albumentations>=1.4,<2.0

# ONNX export (optional, for model size inspection)
onnx>=1.16
onnxruntime>=1.18
